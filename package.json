{
  "name": "collaborative-hero-soundboard",
  "version": "2.0.0",
  "private": true,
  "type": "module",
  "description": "A Socket.IO audience soundboard with host-only audio output.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js",
    "check": "node --check server.js && node --check public/audience.js && node --check public/host.js"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "express": "^5.2.1",
    "socket.io": "^4.8.3"
  }
}
